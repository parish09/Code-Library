#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <bits/stdc++.h>
#include <algorithm>
#include <math.h>
#include <stdio.h>
using namespace std;
int main(){
    int n,x,l,r,c=0,j=1;cin>>n>>x;
    for(int i=0;i<n;i++){
        cin>>l>>r;
        while(j+x<=l){
        if(j+x<=l){
          j=j+x;
        }}
        c=c+r-j+1;
        j=r+1;
    }
cout<<c;
}